{
  "name": "trade-finance-e2e",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "wdio run wdio.conf.js",
    "test:qa": "ENV=qa wdio run wdio.conf.js",
    "playwright": "npx playwright test",
    "report": "allure generate reports/allure-results --clean && allure open"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.0",
    "@wdio/cli": "^9.0.0",
    "@wdio/local-runner": "^9.0.0",
    "@wdio/playwright-service": "^9.0.0",
    "@wdio/cucumber-framework": "^9.0.0",
    "@wdio/spec-reporter": "^9.0.0",
    "@wdio/allure-reporter": "^9.0.0",
    "dotenv": "^16.4.0",
    "axios": "^1.6.0"
  }
}



That file is package.json — the control center of your automation framework.

It tells Node.js, WebdriverIO, Playwright, CI/CD, and developers:

what this project is

how to run it

which tools it depends on

In enterprise automation, this is non-negotiable infrastructure.

// What is this file?

package.json is the Node.js project manifest.

// Defines the project identity
//Defines execution commands
//Defines dependencies and versions
//Ensures consistent installs across machines & CI




